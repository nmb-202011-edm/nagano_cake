<% if admin_signed_in? %>
  <div class="bg-dark pb-4 ml-auto text-right">
    <%= text_field_tag :search, params[:search], placeholder: "Search", class: "text-center, size: 30" %>
    <span class="input-group-btn"><%= button_tag "検索", :name => nil, class: "mr-5 btn btn-primary" %></span>
  </div>
<% end %>
